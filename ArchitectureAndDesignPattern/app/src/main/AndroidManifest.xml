<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="spm.architecture.designpattern">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchitectureAndDesignPattern">
        <activity android:name=".volleyWithMvp.VolleyMvpActivity"></activity>
        <activity android:name=".cleanArchitecture.CleanArchitectureActivity" />
        <activity android:name=".solidPrinciple.SolidPrincipleActivity" />
        <activity android:name=".mvvmWithDataBinding.MvvmDatabindingActivity" />
        <activity
            android:name=".mvvm.MvvmActivity"
            android:label="@string/title_activity_mvvm" />
        <activity android:name=".mvp.MvpActivity" />
        <activity
            android:name=".mvi.MviActivity"
            android:label="@string/title_activity_mvi" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>